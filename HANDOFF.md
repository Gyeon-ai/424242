# 자희 핀볼 인수인계

작성일: 2026-08-27  
프로젝트 경로: `C:\Users\swkim260505\Documents\방셀 수집\roulette-gyeonjahee-001`  
GitHub 저장소: <https://github.com/Gyeon-ai/424242>  
서비스 주소: <https://gyeon-ai.github.io/424242/>  
UI 기준 커밋: `6592129` (`Adjust panel gradient to blue 70 gold 30`)

## 1. 프로젝트 개요

자희 핀볼은 Marble Roulette를 기반으로 만든 클라이언트 전용 핀볼 추첨기다. Box2D 물리 엔진으로 참가자 공을 굴리고 골인 순서에 따라 당첨자를 결정한다. 서버나 데이터베이스는 없으며 정적 파일만으로 동작한다.

현재 제품명은 `자희 핀볼`이고, SOOP 견자희 구독 페이지에서 가져온 장미단 퍼스나콘을 공 이미지와 파비콘으로 사용한다. 화면은 항상 다크 모드이며 스킬은 항상 활성화되어 있다.

## 2. 현재 구현된 기능

- 쉼표 또는 줄바꿈으로 참가자 입력
- `이름*3` 형식으로 동일 참가자의 공 개수 지정
- `이름/3` 형식으로 공 무게 지정
- 첫 번째, 마지막, 사용자 지정 순위 당첨 방식
- 운명의 수레바퀴, 버블팝, 욕망의 항아리, 밤을 달리다 맵
- 참가자 이름을 해시하여 같은 이름에 항상 같은 퍼스나콘 배정
- 공 이미지 회전 및 고품질 캔버스 보간
- 경기 중 1x, 2x, 5x 배속을 즉시 변경하는 미니 오버레이
- 당첨 후 중앙에 나타나는 카운트다운 설정창
- 카운트다운 직접 시작, 일시정지/계속, 리셋
- `?names=이름1,이름2` URL 파라미터로 명단 전달
- 명단을 `localStorage`의 `gyeonjahee_mbr_names` 키에 저장
- PWA 서비스 워커와 오프라인 정적 리소스 캐시

기본 참가자는 `견자희,장미단,핀볼`이다.

## 3. 현재 디자인 사양

- 전체 테마: 고정 다크 모드
- 설정창 위치: 왼쪽 아래
- 경기 중 배속창: 미니맵 오른쪽 바깥
- 타이머창 위치: 화면 상단 중앙, 가로 466px·세로 160px 기준
- 패널 색상 비율: 위쪽 파랑 70%, 아래쪽 금색 30%
- 색상 전환: 70% 지점부터 금색으로 전환하고 82% 이후 금색이 뚜렷해짐
- 설정창은 `--hair-settings-gradient`, 타이머창은 `--hair-panel-gradient` 사용
- 네온 오브젝트 색상: 금색 `#edc886` (`HAIR_NEON_GOLD`)
- 미니맵 카메라 테두리: 파랑에서 금색으로 내려가는 별도 그라데이션
- 일반 버튼: 진한 남색 배경과 밝은 글씨

테마 관련 수정 위치:

- `assets/style.scss`: 설정창, 배속창, 타이머, 버튼, 반응형 배치
- `src/data/constants.ts`: 네온, 카메라 테두리, 다크 테마 색상
- `src/minimap.ts`: 카메라 표시선 그라데이션 렌더링

## 4. 퍼스나콘 이미지

실제 서비스에서 사용하는 이미지는 `assets/personacons-final/`에 있다. 현재 24개이며 구독 개월은 다음과 같다.

`01, 02, 03, 06, 10, 12, 18, 24, 30, 36, 42, 48, 54, 60, 66, 72, 78, 84, 90, 96, 102, 108, 114, 120`

이미지 처리 기준:

- 대부분 Waifu2x 4배 업스케일 결과 사용
- 102개월 이미지는 비교 후 Real-ESRGAN 계열 결과를 사용
- 런타임에서 원본을 다시 2배 캔버스에 캐시하고 `imageSmoothingQuality = 'high'`로 렌더링
- 이름별 이미지는 문자열 해시로 고정되며 같은 이름은 항상 같은 이미지를 받음
- 공과 당첨 결과 이미지 모두 회전값을 반영함

이미지 폴더 역할:

- `assets/personacons-final/`: 실제 서비스용 최종본
- `assets/personacons/`: 추출한 원본 이미지
- `assets/personacons-upscaled/waifu2x/`: Waifu2x 비교본
- `assets/personacons-upscaled/realesrgan/`: Real-ESRGAN 비교본
- `assets/personacons-upscaled/102-variants/`: 102개월 후보 비교본
- `upscale-preview.html`: 업스케일 후보 확인용 페이지이며 서비스 진입점은 아님

새 이미지를 적용할 때는 `src/rouletteRenderer.ts`의 `PERSONACON_URLS`도 함께 수정해야 한다.

## 5. 주요 코드 구조

- `index.html`
  - 설정 UI와 타이머 HTML
  - 명단 처리, 배속 버튼, 타이머 상태 관리
  - 당첨 이벤트 이후 타이머 설정창 표시
  - 페이지 시작 시 다크 모드와 1배속 설정
- `src/index.ts`
  - 앱 진입점과 전역 Roulette 초기화
- `src/roulette.ts`
  - 60Hz 고정 물리 스텝
  - 경기 시작/리셋, 당첨 판정, 공 제거, 배속 적용
- `src/physics-box2d.ts`
  - Box2D 생성, 물리 스텝, 공/맵 충돌체 관리
- `src/marble.ts`
  - 공 위치, 회전, 스킬 쿨타임과 발동 확률
- `src/rouletteRenderer.ts`
  - 메인 캔버스, 공/당첨 화면, 퍼스나콘 캐시와 고품질 렌더링
- `src/data/maps.ts`
  - 핀볼 맵 좌표와 오브젝트 정의
- `src/data/constants.ts`
  - 물리·화면 공통 값과 테마 색상
- `src/minimap.ts`
  - 미니맵과 카메라 표시선
- `assets/style.scss`
  - 전체 UI 스타일과 반응형 레이아웃
- `scripts/build-sw.js`
  - Workbox 서비스 워커 생성
- `.github/workflows/deploy.yml`
  - GitHub Pages 자동 빌드·배포

## 6. 물리, 배속, 성능

물리 계산은 `src/roulette.ts`의 `_updateInterval = 1000 / 60`으로 60Hz 고정이다. 화면 렌더링은 `requestAnimationFrame`을 사용하며 물리와 렌더링이 분리되어 있다.

1x, 2x, 5x는 프레임 제한을 바꾸는 기능이 아니라 한 렌더 프레임 안에서 처리할 물리 시간을 늘린다. 따라서 5x에서 공이 많으면 물리 스텝 횟수가 크게 증가한다. 약 1,900개 공처럼 극단적으로 많은 입력은 CPU 부하가 커지고 프레임이 떨어질 수 있다. 현재 다음 기능은 없다.

- 기기 성능에 따른 자동 배속 제한
- 공 개수에 따른 적응형 물리 주파수
- 원거리 공 렌더링 생략 또는 LOD
- 대량 공 전용 단순 충돌 모델

`_elapsed`가 100ms를 넘으면 나머지만 유지하므로 매우 느린 프레임에서는 시뮬레이션 시간이 일부 유실될 수 있다. 대량 공과 5x 조합을 개선하려면 이 부분을 포함해 별도 성능 설계가 필요하다.

## 7. 공 제거 안정성

과거 검토했던 `setTimeout` 기반 골인 공 제거 방식은 현재 코드에 없다. 지금은 `_updateMarbles()`에서 골인 공 ID를 `removalIds`에 모은 뒤 같은 물리 프레임에서 `physics.removeMarble(id)`를 호출한다.

따라서 이전 라운드의 지연 제거 타이머가 새 라운드의 동일 ID 공을 지우는 문제는 현재 구조에서는 발생하지 않는다. `index.html`에 남은 3초 `setTimeout`은 당첨 후 설정창 레이아웃을 복귀시키는 UI 타이머이며 물리 공 제거와 무관하다.

## 8. 타이머 동작

당첨이 확정되면 즉시 카운트다운이 시작되지 않고 `타이머 설정` 창이 나타난다.

1. 분과 초를 숫자로 입력한다.
2. `시작`을 눌러야 카운트다운이 시작된다.
3. `일시정지`를 누르면 남은 시간이 고정되고 버튼이 `계속`으로 바뀐다.
4. `리셋`을 누르면 타이머가 중단되고 시간 설정 화면으로 돌아간다.
5. 0분 0초는 시작되지 않는다.
6. 초 입력은 0~59로 보정된다.

타이머는 `Date.now()` 기준 종료 시각을 사용하므로 단순 인터벌 누적 방식보다 브라우저 지연에 강하다.

## 9. 현재 주의사항과 후속 개선 후보

### 경기 간 배속 유지

페이지 최초 초기화 시에는 `setSpeed(1)`이 실행된다. 하지만 한 경기에서 5x를 선택한 후 새 경기를 시작하면 배속이 5x로 유지된다. 매 경기 기본 배속을 1x로 만들려면 `btnStart` 처리 직전이나 당첨 처리 시점에 `setSpeed(1)`을 추가해야 한다.

### 대량 공 성능

공 수가 수백~수천 개이면 Box2D 충돌 계산, 정렬, 텍스트와 이미지 렌더링이 동시에 증가한다. 특히 5x에서 부하가 급격히 커진다. 성능 개선은 공 개수 제한, 텍스트 생략, LOD, 물리 서브스텝 상한 중 하나를 먼저 결정하는 것이 좋다.

### 서비스 워커 캐시

배포 직후 이전 CSS나 JS가 잠깐 보일 수 있다. 서비스 워커가 `StaleWhileRevalidate`를 사용하기 때문이다. 확인할 때는 강력 새로고침 또는 `?verify=커밋해시` 같은 쿼리를 붙인다.

### README 내용

현재 `README.md`에는 이전 타요 핀볼 설명과 타빡이 퍼스나콘 정보가 남아 있다. 서비스 코드에는 영향이 없지만 공개 저장소 문서 정리 시 `자희 핀볼`, 장미단 퍼스나콘 24종 정보로 갱신해야 한다. 이 `HANDOFF.md`가 현재 구현 기준으로 더 정확하다.

### 이미지 권리

퍼스나콘은 SOOP 구독 페이지에서 제공된 이미지다. 코드 라이선스와 별도로 이미지 사용·배포 권리는 원저작권자 정책을 확인해야 한다.

## 10. 개발과 검증

요구 환경:

- Node.js 20 이상
- pnpm 11.19.0 권장

설치 및 개발 서버:

```powershell
pnpm install
pnpm dev
```

기본 개발 주소는 `http://localhost:1235`다.

배포 빌드:

```powershell
pnpm build
```

빌드 결과는 `dist/`에 생성되며 이 폴더는 Git에서 제외되어 있다. 빌드 결과 미리보기:

```powershell
pnpm preview
```

기본 미리보기 주소는 `http://localhost:4173`이다.

수정 후 최소 확인 항목:

1. `pnpm build` 성공
2. 브라우저 콘솔 오류 없음
3. 설정창의 파랑 70%·금색 30% 비율
4. 경기 시작 후 금색 네온과 배속창 위치
5. 1x, 2x, 5x 즉시 전환
6. 당첨 후 타이머 설정창 표시
7. 타이머 시작, 일시정지/계속, 리셋
8. 퍼스나콘 공 회전과 당첨 이미지 품질
9. 미니맵 카메라 테두리 위치와 색상
10. GitHub Pages 공개 주소에서 정적 파일 404 없음

## 11. GitHub 배포 흐름

1. 변경 사항을 `main` 브랜치에 커밋하고 푸시한다.
2. `Build and Deploy` GitHub Actions가 pnpm 의존성을 설치하고 `pnpm build`를 실행한다.
3. `peaceiris/actions-gh-pages@v4`가 `dist/`를 `gh-pages` 브랜치에 배포한다.
4. GitHub의 `pages-build-deployment`가 실제 Pages 주소를 갱신한다.

GitHub Pages 설정은 `gh-pages` 브랜치의 `/ (root)`를 소스로 사용한다. 일반적인 푸시 명령은 다음과 같다.

```powershell
git add .
git commit -m "변경 내용"
git push origin main
```

배포 확인 위치:

- Actions: <https://github.com/Gyeon-ai/424242/actions>
- Pages: <https://gyeon-ai.github.io/424242/>

## 12. 인수 직후 권장 작업

1. `README.md`의 타요 핀볼 잔여 문구를 자희 핀볼 기준으로 정리한다.
2. 매 경기 시작 시 배속을 1x로 초기화할지 사용자에게 확인한다.
3. 실제 방송에서 사용할 최대 공 개수를 정하고 대량 공 성능 기준을 만든다.
4. 이미지 배포 권리와 출처 표기를 최종 확인한다.
5. 기능 변경 전 현재 커밋과 Pages 화면을 기준점으로 남긴다.
